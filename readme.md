### Swissborg data guardian

- Run dev server: `yarn dev`
- Build for production: `yarn run build`
- Output directory: `/dist`
- Environment variables:
  - `VITE_GRAPHQL_SERVER`: (example: `http://api-stage-swissborg.ulproto.dev`)
